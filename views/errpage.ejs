<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Page</title>
    <%- include('./partials/cssCDN', { innerRoute: false }) %>
</head>
<body>
    <%- include('./partials/navbar') %>
    <% if (locals.errMsz) { %>    
        <div class="container mt-2" style="display: flex; justify-content: center; color: blueviolet;">
            <h1> <%= errMsz %> </h1>
        </div>
    <% } %>
    <br>
    <button onclick="back_func()" class="btn btn-primary" style="margin-left: 43%;">Back to Home</button>
</body>
<%- include('./partials/scriptCDN') %>
<script>
    function back_func() { window.location.href = "http://localhost:<%= PORT %>"; }
</script>
</html>